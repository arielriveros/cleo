# Changelog

Versions follow the scheme `1.x.y.z`, where **x** counts breaking changes, **y** counts major features
and **z** counts fixes and refactors. The leading `1.` is a fixed product-line prefix; `package.json`
stores the remaining `x.y.z` because npm requires three-component semver.

Sections are generated by `tools/changelog.mjs` from the commits between two release tags.

## v1.1.0.0 — 2026-08-30

The WebGPU release. The renderer now runs on a hardware abstraction with two complete backends, and
the editor can drive its viewport on either. Lighting moved to real photometric units on a physical
BRDF, and the model pipeline gained LOD generation and a storage format that survives large projects.

### Breaking changes

- Lights are authored in photometric units: lux for directional, lumens for point and spot, with a
  real `range` that also serves as the culling radius. Old scenes, published bundles and examples
  migrate on load, but **a scene saved by 1.1 cannot be read by 1.0**. Migrated lights carry a
  `legacyFalloff` marker and deliberately large numbers; the light inspector says so and offers a
  reset to physical defaults.
- `light.ambient` is gone. Ambient is now `Scene.ambientLight`, in lux, with a colour and intensity
  control in Scene Settings. Recovered from old saves automatically.
- Published games must be republished: `PLAYER_CONTRACT` is now **6**. A skinned model's
  `jointIndices` / `jointWeights` moved out of the manifest JSON into `game.bin` blob chunks. A v5
  player loading a v6 bundle gets skinned characters with no bone weights; `publishClient` refuses the
  mismatch rather than shipping it.
- The model library is stored one record per model with geometry as typed arrays, instead of a single
  array rewritten on every edit. Projects migrate forward automatically, one asset at a time — **a
  project opened in 1.1 will not open in 1.0**.
- `Renderer.screenshot()`, `screenshotOffscreen()` and `renderProbePreview()` return
  `Promise<string>`; WebGPU readback is asynchronous. `screenshot()` is deprecated in favour of
  `screenshotOffscreen()`.
- `Mesh.draw()`, `drawRange()` and `drawInstanced()` throw by name on a non-WebGL2 device instead of
  dereferencing an undefined context. Direct `mesh.draw()` calls are WebGL2-only by contract.
- The G-buffer normal target is octahedral-packed — `rg` normal, `b` reflectance, `a` roughness.
  Screen-space custom materials that read it directly need updating; deferred custom materials fill a
  `Surface` struct and are unaffected.
- A deferred custom material is now skipped, with one warning, in forward passes and light-probe
  capture. It previously drew silently wrong.
- Engine shaders are authored in WGSL; the `shaders/deferred/`, `shaders/environment/` and
  `shaders/ibl/` GLSL trees are deleted and GLSL ES 300 is generated at build time. Forks or patches
  against those paths will not apply.
- Bloom works for the first time — the compose target carried a mip chain nothing generated. Defaults
  were re-tuned with it (threshold 1.0 → 2.0, intensity 0.6 → 0.35), so existing scenes will look
  different.
- Terrain relief is switched off (`TERRAIN_RELIEF_ENABLED = false`) and terrain height authoring is
  hidden. Deferred rather than abandoned; parallax occlusion on ordinary materials is unaffected.
- Lights beyond `MAX_POINT_LIGHTS` / `MAX_SPOTLIGHTS` are excluded rather than running off the end of
  the uniform array.
- `npm run build` now also emits `.d.ts` declarations, and `npm run naga:build` regenerates the
  vendored WGSL translator.

### Features

- **WebGPU backend.** A render hardware interface with two complete implementations — WebGL 2 and
  WebGPU — behind one device abstraction. The Renderer panel has a backend picker; the choice is a
  machine-local preference applied on reload. The editor viewport draws on either backend. Published
  games still run on WebGL 2.
- **Custom materials run on WebGPU** in all three modes — forward, deferred and screen. Your GLSL is
  translated by a vendored `naga` build, and the material inspector reports the verdict, quoting
  naga's own diagnostic when a shader cannot cross over. New `screenUV()` helper for screen-space
  materials, with (0,0) bottom-left on both backends.
- **Photometric lights** — lux and lumens, windowed inverse-square falloff, and `sourceRadius` /
  `angularRadius` for area-like highlights and sun-size-scaled shadow softness.
- **A physical BRDF** — height-correlated Smith visibility, Burley diffuse, per-material reflectance,
  and Kulla-Conty multi-scatter compensation so rough metals stop reading dull.
- **Auto-exposure.** The camera meters the frame and drives exposure itself, with EV min/max clamps,
  compensation in stops, and separate adapt-up and adapt-down rates. On by default.
- **Specular occlusion, horizon occlusion and specular anti-aliasing** — reflections survive in
  corners, the sky-rim on normal-mapped surfaces is gone, and sharp highlights stop flickering.
- **Sky light.** A scene singleton that projects the sky — a baked atmosphere or a skybox — into nine
  L2 spherical-harmonic coefficients, so a surface facing up receives the zenith and one facing a
  sunset receives the sunset. Cloud cover grades it.
- **Parallax occlusion mapping** on PBR forward and deferred paths, with the height map promoted to
  its own texture slot on every material type.
- **Alpha mask and cutout shadows** — `maskMap` and `alphaCutoff` on all three shading models, and
  four new depth programs so a masked surface casts the silhouette it actually draws.
- **Emissive HDR**, including glTF `KHR_materials_emissive_strength`, plus terrain ambient occlusion
  and a final-image saturation grade.
- **LOD generation** in the model editor: an in-house quadric-error decimator with half-resolution
  texture twins shared between levels of the same size.
- **Sub-mesh grouping on import.** Separate and Merge stopped being mutually exclusive — enable both
  to partition an import's parts, each group becoming one asset with its members merged.
- **Foliage at scale.** A density falloff that thins instance counts by distance, a 4M-triangle draw
  budget that stops a heavy prototype removing the GPU device, and a hysteresis band plus amortised
  cell admission so moving the camera no longer spikes. Layers are keyed by a stable id, so renaming
  one no longer orphans its instances.
- **The Renderer panel split in two** — a Performance panel with per-pass CPU *and* GPU columns,
  frame-budget lines and per-pass kill switches that measure marginal cost, and a Renderer Settings
  panel. GPU timers now work on both backends, and a new CPU profiler times the same scopes.
- **Renderer settings persist.** Every control now marks the scene changed, so a tuned look survives a
  refresh — and reaches publish and export, which carry the same snapshot.
- Terrain material previews render on a real patch of terrain scaled to a reference landscape rather
  than on a sphere, and the Add palette gains a Plane primitive.

### Fixes & refactors

- **Cross-backend correctness.** Zero-to-one clip depth on WebGPU; the screen-space Y flip folded into
  the reconstruction and shadow-lookup matrices; mip chains, cube-face bakes and probe captures
  generated the right way up; channel packing no longer vertically mirrored, which had been reading
  authored metallic/roughness maps upside down.
- **Custom materials.** A material whose source never read a declared uniform silently failed to draw;
  a uniform name declared in two blocks is now written to both, which had left forward custom
  materials rasterising nothing at all with the right draw count and no error.
- **Render passes and resources.** Every colour attachment is cleared, not just the first; the forward
  clear is a real pass; depth textures are declared as depth textures and bound only where sampled;
  texture upload address modes are honoured; each draw gets its own uniform slot; WebGL2 pipelines are
  keyed by vertex layout.
- **Instancing.** Each instanced group gets its own buffer, so a second group in a pass no longer
  reads another's matrices or references a destroyed buffer.
- **Shader validity.** Four shaders sampled textures from non-uniform control flow, which WGSL
  rejects — and one rejected module cost the terrain, the clouds and the selection outline all of
  their pixels.
- **SSAO** read the G-buffer normal through a linear sampler, so every normal was a bilinear blend;
  switching SSAO off left the previous frame's occlusion bound, so the kill switch stopped the work
  and left the effect.
- **Auto-exposure** readback placement was causing context loss, and its adaptation speeds carried
  Unreal's names with the meaning inverted, adapting nine times slower than the numbers implied.
- Empty and degenerate draws are skipped rather than recorded; the Performance panel's Physics and
  Scene rows read a field that did not exist and always showed 0.00 ms.
- Internally: comment volume across `src/` and `editor/src/` cut ~37%, `EngineContext.tsx` split into
  five hooks and four modules, and 54 new unit-test files added.

## v1.0.156.27 — 2026-08-21

### Features

- feat: enhance asset bundling and testing (c8e3f88)
- feat(graphics): add support for texture2DArray in TextureConfig and implement depth texture array functionality (5d2516e)
- feat: vscode integration of editor vfs (02eac7f)
- feat: add support for parsing .fbx/.glb files as glTF2 (20ac581)
- feat: Add primitive and complex geometries, change icon ecosystem (73dac75)
- feat: add node name validation and scoped drag-and-drop manager (29b317e)
- feat: overhaul ui functionality and edition (543dd32)
- feat: implement SSAO with depth-aware upsampling and kernel generation (344c3ea)
- feat: add support for 3D textures and update shaders for volumetric clouds (69afd13)
- feat: add example projects gallery and import functionality (85ad9f2)
- feat: implement tilemap and tileset editors (279e728)
- feat: implement project management features including project launcher, explorer, and modal (8d3eb67)
- feat: Add tests for IK rig handling, animation state capture, and skeleton topology (c0cbc6d)
- feat: add terrain data compression and skeleton topology management (7aa13cd)
- feat: enhance AnimationSlot to support any skinned model and improve asset linking functionality (63e4fa5)
- feat: add root motion support to animations and update volumetric clouds rendering (81af10f)
- feat: Add ground probe distance to RigidBody and update related physics systems (9c0cdb0)
- feat: Implement debug visibility context and menu for overlay toggles (3c098ec)
- feat: Add probeable property to materials and implement probe preview functionality (bb8a78f)
- feat: Implement bone name normalization and humanoid slot mapping for animation retargeting (492701b)
- feat: Animation field (4211648)
- feat: implement node JSON serialization and cloning utilities (6414376)
- feat: Enhance scene change event handling and editor state management (b67576b)
- feat: enhance project bundle handling with preferences and streamline import/export functionality (ca07134)
- perfor: Modify camera rig collision handling and improve bounding box calculations (e6c80f0)
- feat: add CameraRigNode support to Scene and implement camera rig updates (f64efb0)
- feat: enhance script export handling and improve physics material management (025f9d2)
- feat: enhance script analysis and hover functionality (bafbf2c)
- feat: add script snippets and provider for Monaco editor (a986dcd)
- feat: implement bundle export and import functionality (f5f60e8)
- feat: implement scene asset, add project configuration export and import functionality (c1f4f94)
- feat(graphics): enhance deferred lighting with IBL support and volumetric effects (2b8f8b9)
- feat: Enhance GLTF loading and texture management (ce4d16c)
- feat: enhance node expression resolution and script linting with NodeResolver integration (e88c50a)
- cicd: correct npm script commands for editor installation and build in CI workflows (5ad7361)
- feat: Implement code editor enhancements and scene inspector improvements (0d3716a)
- feat: integrate console-feed for enhanced logging experience (a54e4e6)
- feat: Refactor dock layout and panel structure for improved organization and functionality (cb9aed8)
- feat: Implement dock layout system with customizable panels (5b3b9de)
- feat: Enhance asset thumbnail rendering and preview scenes (4a13472)
- feat: Implement asset upload routing and drag-and-drop functionality (21762ad)
- feat: enhance input manager for pointer lock detection, improve physics body shape attachment, and add convex hull generation (aebc43e)
- feat: add custom material support with user-defined shaders (b1ee0a6)
- feat: add terrain material management features, improve lighting render techniques, enhance animation data management (c6c987e)
- feat: Add SkyAtmosphereNode and related rendering features (5573c7a)
- feat: add volumetric clouds node and rendering support (9bd7e58)
- feat(renderer): Implement render settings management for consistent editor and standalone game visuals (61b1e7b)
- feat(animation): Enhance state machine editor with variable parameter binding (240ca39)
- feat(renderer): add motion blur functionality and skeleton overlay support (2b46a91)
- feat: Enhance material and mesh management with multi-select and batch delete functionality (4e481a7)
- feat: add gizmo mode functionality for transform operations (60cc39a)
- feat: add mesh asset management and exploration features (474abca)
- feat: add foliage cell size option for improved culling control (ced2b47)
- feat: enhance renderer with frustum culling and foliage distance culling options (a968fef)
- feat: implement frustum culling and bounding sphere calculations for improved rendering performance (5aa45f2)
- feat: add performance statistics tracking for rendering and integrate with editor HUD (d445577)
- feat: add Material editor mode with reusable material assets (75d9f0e)
- feat: implement tab management with draggable and closable tabs for the editor (9743a5c)
- feat: add renderer mode with debug view options and enhance editor functionality (4c98d69)
- feat: enhance property editor with read-only support for template instances (f65f399)
- feat: Implement Image-Based Lighting (IBL) and Screen-Space Ambient Occlusion (SSAO). Implement first iteration of ragdoll physics for Skinned Meshes (0ea69c6)
- feat: improve editor functionality with grid visibility toggle and enhanced GLTF loading (4d275ea)
- feat: enhance publishing process with script extraction and asset packing (0158704)
- feat: add Electron desktop application for Cleo engine with publishing capabilities (e94659d)
- feat: enhance terrain system with foliage support and texture serialization (9c5030f)
- feat: engine loading screen, extra cleaning (67f66cc)
- feat: dynamic UI implementation, UI editor and scripting (0623d90)
- feat: Add AssetExplorer component and integrate texture management with drag-and-drop support (7bf1633)
- feat: Implement mouse capture functionality for play mode and enhance input management (90b65e6)
- feat: Add support for animated sprites with editor integration and UV transformation (cdd6ea0)
- feat: Implement PBR material support and refactor material handling (175539b)
- feat: enhance MaterialEditor with shader selection and properties management (b65076f)
- feat: migrate to tailwind css (5d4fc21)
- feat: Add interpolation between degrees of movement in demo, fix implicit any (aac8411)
- feat: Implement animation direction vector for correct triggers (bcb100b)
- feat: Add Animation Editor and integrate animation mapping functionality in ModelNode and Animator (577f201)
- feat: Add update method to ModelNode for animator updates and remove it from Scene (2bc8c2f)
- feat: Add animator and enhance animated model support with skinning and node transformation management (3ec7e6c)
- feat: Implement skinned model support with new shaders, mesh initialization, and GLTF loading enhancements (1b49418)
- feat: Enhance input handling by capturing wheel events only when mouse is over the canvas and add mouse over detection method, fix texture loading issues with loader (2728f4f)
- feat: Implement animated model support with GLTF loading and enhance loader functionality (8ba1e66)
- feat: Add support for GLTF model loading and enhance texture management (4791751)
- Enhance demo scene setup by adding collectibles and hazards with corresponding triggers and scripts. Refactor node removal logic to include onDespawn handling for better cleanup. (3275fac)
- Add Sponza assets and implement environment mapping in demo scene (7adde7d)
- Enhance asset management and scene setup: - Implement createDemoScene function for initializing demo scenes with camera, grid, axes, and a damaged helmet model. - Refactor EngineContext to utilize createDemoScene for better organization and performance. - Update InputManager to capture mouse wheel events for zoom functionality. (6bbce52)
- Add copy-webpack-plugin for asset management in webpack configuration and update package dependencies (5253433)
- Implement asset path resolution for local and deployed environments in loader and assimpLoader. Update image loading methods to use the new path resolution function for improved asset management. (109e4b1)
- Add damaged helmet model and textures to the editor. Implement loading logic in EngineContext, including physics setup and debug visualization. Update initial scene setup to include the helmet model. Refactor engine initialization to be asynchronous for better performance. (73cb9bd)
- Add gizmo functionality for 3D object manipulation in the editor. Implement PositionGizmo component for positioning nodes, integrate gizmo drag events, and enhance raycasting to include gizmo nodes. Update camera controls to prevent interference during gizmo dragging and ensure proper visibility management in play mode. (3316ef3)
- Implement raycasting for node selection in the editor viewport, add selection outline visualization, and enhance bounding box calculations for various node types. (be7a720)
- node visibility, improved scene changed event (23db9b3)
- editor playable example, input prevent default, reset physics world (551e444)
- code editor contexts for globals and node object (026ad64)
- input in global script state (4500d5b)
- script logging (e177fda)
- error catching, single script per node (44fd1f5)
- static sprites (35d0771)
- editor logging (7c7e6c9)
- editor asset texture selection (d931100)
- implement triggers (5f929d3)
- cylinder geometry/shape, physics constraints (4810179)
- editor debug body shapes and edit (64a945e)
- render spotlights (0e7388f)
- add editor event handlers, resume game, fix: onStart script, onUpdate pause (e73d926)
- 2d view (f81ebbc)
- import models from editor client (c4e4883)
- physics editor (cd062a9)
- orthographic camera & camera editor (7cd3880)
- edit node name, resize sidebars, event emitter, list textures (ac5cc37)
- editor as entrypoint (afffe0b)
- install editor command (091502b)
- editor deployment (90a3714)
- editor grid, axis, camera model (e681f91)
- editor drag and drop node hierarchy (8e63803)
- camera node (a2abd49)
- editor scripting (2cc043e)
- skybox node editor (d628966)
- root behaves properly on change and parsing (4db1ef5)
- scene editor, scene serialize and parse (5efb021)
- import/export textured model as json (e6f03a0)
- texture manager (af60586)
- engine external module build (8fde6fe)
- spawn, update and collision callbacks, body removal, body debug wireframe (11e42e5)
- cubemaps, skybox and material reflectivity (ecab7d9)
- normal mapping, mask mapping, assimp single file (a75f144)
- mesh instancing (9cf4df9)
- switch to cannon-es (781e97e)
- bloom post process (b9887db)
- directional shadow map (109d785)
- framebuffer and screen shader (5dbd002)
- multiple shapes for rigid bodies (7ad9816)
- assimp model loader (f95ceb7)
- rotate using quaternions (006370d)
- rigid body physics (b92cb45)
- transparency (a434d61)
- terrain from heightmap (1e47f8d)
- model emissive channel (78a6565)
- pointlights (01ce76a)
- scene graph (e7c7144)
- input manager (4416f1f)
- obj file model loading (93912c1)
- specular mapping (de4df6d)
- implement texture (060d1f5)
- custom materials (85484bc)
- directional lighting (dbb7601)
- implement perspective camera (89efceb)
- material system and geometry class (223013c)
- implement model transform (94e91a0)
- implement material and model class (57bf035)
- color as a uniform, uniform set in shader (1bb60ca)
- vertex layout handled by shader (12a3867)
- load shaders from files (7444796)
- store attribute info on shader load (3517978)
- implement shader class (5df9ed4)
- draw basic triangle (8bcc556)
- first commit (3c1361f)

### Fixes & refactors

- Refactor material handling for merged models (2a60414)
- refactor: node child classes have their own files (c46e4b8)
- fix: Implement cloud upsampling shader and enhance bloom processing (290b3d5)
- refactor: streamline sprite handling and tileset integration (69b25d9)
- refactor: Material handling and optimize texture packing (6a1330f)
- perf: Enhance shader performance and implement profiler (1dc0287)
- Refactor game publishing process to use game.bin for data storage (1fa2b28)
- perf: Refactor geometric vertex data to use typed arrays (e6008b3)
- refactor: rename MeshAsset to ModelAsset and update related references (ac694fd)
- Refactor graphics loader to require material in model output, ensuring consistency in model data structure. (09c9bdd)
- Refactor animation state machine and improve UI components (897982d)
- style: introduce typography classes for consistent styling across UI components (1332819)
- refactor: unify script handling and improve module imports (bc372b2)
- style: Adjust file area layout for improved responsiveness and density in asset panel (ab6d3ef)
- Refactor code structure for improved readability and maintainability (454e27e)
- Refactor Animation State Machine Editor and Enhance Environment Mapping (ede81a1)
- refactor: update UI components to use design tokens for colors and styles (194e2fb)
- Refactor material types and enhance outline rendering (dc9c379)
- fix: No console clear on tab change, Emit TEXTURES_CHANGED event on Assets tab switch and enhance texture loading feedback (b5f48f3)
- remove  ignore deprecated command, add gltf sample file (8fb49f5)
- Refactor asset path resolution by removing the resolveAssetPath function from loader and assimpLoader. Update image loading methods to directly use the provided path for improved simplicity and performance. (bedae3e)
- Refactor node selection visualization to use stencil-based outlining instead of creating outline nodes. Update bounding box calculations for model nodes and adjust camera bounding box size for improved selection. Add outline shaders for rendering selected nodes and sprites. (e315431)
- fix: world forward, editor light properties (94aec4b)
- update firebase actions (a0ea2d9)
- fix: game rotation uses euler, physics uses quats (ca6102f)
- improve bloom and chromatic aberration pass (db63b91)
- fix cpu bottlneck in uniform updates (5972ddd)
