.vscode
build
dist
node_modules

# Generated standalone game player bundle (built by `npm --prefix editor run build:player`)
editor/public/player/

# vitest coverage reports (root and editor)
coverage/

# Rust build output for the vendored naga WASM artifact. The artifact itself
# (src/graphics/rhi/webgpu/naga/) IS committed — see tools/naga-wasm/README.md.
tools/naga-wasm/target/
